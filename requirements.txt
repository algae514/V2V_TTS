fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
soundfile==0.12.1
numpy>=1.26.0
requests==2.31.0
hf_transfer
soxr

# Note: torch and torchaudio are installed separately in setup.sh
# This allows GPU detection and proper CUDA/CPU installation
# MeloTTS - install from git (main branch)
# Uses default English model from MeloTTS repository
git+https://github.com/myshell-ai/MeloTTS.git@main

